## 3-[1] 배열과 연결 자료구조의 메모리 사용량 비교

### 계산 방법

각 예제에서 두 프로그램에 같은 트리를 입력하고, `sizeof`와 실제 할당 칸 수를 이용해 저장 공간을 계산했다. 이번 실행에서 `sizeof(char)`는 1바이트, `sizeof(ArrayTree)`는 16바이트, `sizeof(Node)`는 24바이트였다. 구조체의 크기에는 메모리 정렬을 위한 빈 공간도 포함된다.

- **배열:** `sizeof(ArrayTree) + capacity × sizeof(char)`
- **연결 구조:** `노드 수 × sizeof(Node)`

배열은 처음에 2칸을 할당하고 필요한 인덱스가 범위를 넘으면 두 배로 늘린다. 루트 높이를 0으로 계산하면 높이 h인 트리의 최종 배열 크기는 `capacity = 2^(h+1)`이다. 사용하지 않는 0번 칸과 중간의 빈 칸도 메모리에 포함된다.

비교 대상은 최종 트리 저장 공간이다. 임시 변수, 재귀 호출, 배열 확장 중의 일시적인 공간, `malloc` 내부 관리 공간은 제외했다.

### 입력 예제와 계산 결과

| 종류 | 입력 |
| --- | --- |
| 일반 | `A(B(D,E),C(F,G(H,)))` |
| 완전·포화 | `A(B(D,E),C(F,G))` |
| 완전·비포화 | `A(B(D,E),C(F,))` |
| 왼쪽 편향 | `A(B(C(D(E(F(G(H(I(J)))))))))` |

| 종류 | 노드 수 | 높이 | 배열 칸 수 | 배열 계산(바이트) | 연결 계산(바이트) |
| --- | ---: | ---: | ---: | --- | --- |
| 일반 | 8 | 3 | 16 | 16 + 16 × 1 = **32** | 8 × 24 = **192** |
| 완전·포화 | 7 | 2 | 8 | 16 + 8 × 1 = **24** | 7 × 24 = **168** |
| 완전·비포화 | 6 | 2 | 8 | 16 + 8 × 1 = **24** | 6 × 24 = **144** |
| 왼쪽 편향 | 10 | 9 | 1,024 | 16 + 1,024 × 1 = **1,040** | 10 × 24 = **240** |

### 결과 설명

완전 이진트리에서는 배열이 더 적은 메모리를 사용했다. 빈 칸이 적고 노드마다 자식 포인터를 저장하지 않아도 되기 때문이다. 다만 0번 칸은 사용하지 않으며, 완전하지만 포화가 아닌 트리에는 마지막 층의 빈 칸도 있다.

일반 예제도 배열이 32바이트, 연결 구조가 192바이트로 배열이 작았다. 이 예제는 높이가 낮고 각 노드에 문자 한 글자만 저장하기 때문이다. 일반 이진트리는 모양이 다양하므로 항상 배열이 작다고 할 수는 없다.

편향 예제는 노드가 10개뿐인데 높이가 9이므로 배열이 `2^10 = 1,024`칸까지 늘어났다. 실제 사용하는 노드는 10개여서 빈 공간이 많다. 연결 구조는 노드 10개만 저장하므로 배열의 1,040바이트보다 적은 240바이트를 사용했다. 따라서 이번 구현에서는 빈 칸이 적은 트리는 배열이, 높게 편향된 트리는 연결 구조가 메모리 사용에서 유리했다.

## 3-[2] 자식·부모·형제 조회 프로그램의 효율 비교

### 조회 방법

배열은 입력한 이름을 1번 칸부터 찾아 인덱스 i를 얻은 뒤 다음 식을 사용한다.

| 관계 | 구하는 방법 |
| --- | --- |
| 왼쪽 자식 | `2 * i` |
| 오른쪽 자식 | `2 * i + 1` |
| 부모 | 정수 나눗셈 `i / 2` |
| 형제 | i가 짝수이면 `i + 1`, 홀수이면 `i - 1` |

계산한 위치가 배열 범위 안에 있고 실제 노드가 있는지 확인한다. 루트에는 부모와 형제가 없고, 자식이나 형제가 없는 경우에는 없다고 출력한다.

연결 구조는 현재 노드 → 왼쪽 → 오른쪽 순서로 이름을 찾는다. 이때 부모 포인터를 함께 전달하므로 원하는 노드를 찾으면 부모도 알 수 있다. 자식은 `left`, `right`로 확인하고, 형제는 부모의 다른 쪽 자식으로 구한다. 부모를 찾기 위해 다시 순회하지 않는다.

### 예제와 효율 분석

`A(B(D,E),C(,F))`에서 C를 조회하면 두 프로그램 모두 왼쪽 자식 없음, 오른쪽 자식 F, 부모 A, 형제 B를 출력한다.

n은 실제 노드 수, capacity는 배열 칸 수이다.

| 과정 | 배열 | 연결 구조 |
| --- | --- | --- |
| 이름으로 노드 찾기 | 최악 O(capacity) | 최악 O(n) |
| 검색 후 자식·부모·형제 출력 | O(1) | 함께 찾은 부모를 이용하므로 O(1) |
| 전체 조회 | 최악 O(capacity) | 최악 O(n) |

위 예제의 C는 배열의 3번에 있으므로 배열은 3칸을 검사한다. 연결 구조는 A, B, D, E, C 순서로 5개 노드를 방문한다. 반면 왼쪽 편향 10개 노드에서 마지막 J는 배열 인덱스 512에 있으므로 배열은 512칸을 검사하고 연결 구조는 10개 노드를 방문한다. 이 수는 코드의 탐색 순서에 따른 횟수이며 실행 시간 측정값은 아니다.

따라서 이름으로 조회할 때는 트리 모양과 노드 위치에 따라 결과가 달라진다. 빈 칸이 많은 트리는 연결 구조가 유리하다. 배열은 인덱스를 이미 알고 있다면 검색 없이 부모와 자식을 바로 계산할 수 있다는 장점이 있다. 연결 구조는 노드 포인터만 알고 부모 정보를 모르는 경우 부모를 찾는 탐색이 필요하지만, 이번 프로그램은 이름 검색 중 부모를 함께 구하여 이 비용을 줄였다.
